# Core pipeline
markitdown[pdf]>=0.1.0     # PDF and document to Markdown conversion
anthropic>=0.25.0           # Claude API client
instructor>=1.0.0           # Structured LLM output with automatic retry
pydantic>=2.0.0             # Schema definition and validation

# PDF scan detection (used alongside MarkItDown)
pymupdf>=1.24.0             # PDF text layer detection only

# Utilities
python-dotenv>=1.0.0        # Load ANTHROPIC_API_KEY from .env file

# Testing
pytest>=8.0.0
